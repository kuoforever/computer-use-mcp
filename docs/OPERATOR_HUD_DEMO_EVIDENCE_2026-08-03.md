# Post-fix Chrome-to-Word Operator HUD Demo

> **Result: PASS — retained real-environment run on 2026-08-03.**

## Claim

The bounded Chrome-to-Word Demo completed end to end after all three Presence
halo fixes. It used the existing Runner and stdio project MCP as the only
desktop dispatch path, one fixed public Microsoft Support page, one disposable
Word fixture, the rebuilt Decision Card, the six-chapter Progress HUD, and the
capture-excluded Presence halo.

This is bounded application and HUD evidence. It is not a universal-GUI,
provider, model, authenticated-browser, or general Office capability claim.

## Retained run

| Field | Value |
| --- | --- |
| Run ID | `cross-app-demo-20260803-024517-764321` |
| Result | `PASS` |
| Durable phase | `SUCCESS` |
| Tool calls | `17 / 17` |
| Approved side effects | `7 / 7` |
| Model turns | `18 / 20` |
| Run duration | `61,901 ms` |
| Document SHA-256 | `48d8393e70e9305dfb4aa8537a1a9d49aa2d1eb18202a55a422c008036c52629` |
| Dispatch | Existing Runner and stdio project MCP only |

The saved DOCX is a valid 19-entry package. Reading `word/document.xml`
independently finds the fixed `VERIFIED PORTAL FOLLOW-UP` marker. The durable
checkpoint records `phase=SUCCESS`, 17 successful tool calls, seven approved
side effects, zero tool failures, and ten verified observation epochs. The
redacted trace retains no raw typed text.

## Presence evidence

Presence remains `WDA_EXCLUDEFROMCAPTURE`; it was not made capturable and no
screenshot is claimed. The run-owned probe in `final-state.json` records:

| Probe | Result |
| --- | ---: |
| Projection count | 85 |
| Painted samples | 247 |
| Unpainted samples | 0 |
| Window-absent samples | 32 |

The projection sequence contains all five expected phase/authority states,
including seven `WAITING_APPROVAL/WAITING` boundaries. Every active phase and
approval yield reached painted window state. This closes the post-fix complete
run requested by the 2026-08-02 handoff without relying on a screenshot of a
capture-excluded surface.

## Cleanup and remaining operator evidence

Cleanup issued one close request to each exact launched fixture. Chrome and
Word both reached `windows_closed`; a fresh window inventory afterward showed
neither disposable window and preserved the operator's unrelated applications.

This run does not establish the two remaining operator-only acceptance items:

- Decision Card appearance at 100% and 125% display scaling;
- a physical Alt+Tab press while a Decision Card is open.

Those items remain separate and must not be synthesized or inferred from this
run.
